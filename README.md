# cluade-agent-constructor-copilot

A minimal VS Code extension scaffold that opens a Scratch-inspired canvas for composing agent, skill, and script blocks and exporting them as markdown files in `/agent-constructor-output`.
